// Persistence keys. Device geometry now lives with the rendering code
// (studio/canvas/device-shell.tsx) and the layout blueprints, so there are no
// magic frame numbers to keep in sync here.

export const DOC_STORAGE_KEY = "vitrine:studio-doc:v2";
export const DOC_FILE_NAME = "vitrine.project.json";
